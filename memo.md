check docker version first: ```docker --version```

```git clone https://github.com/MINJILEE-PURDUE/rpl_vision.git```

```cd rpl_vision.git```

``` sudo docker build -t {} . ``` 
``` sudo docker run -it {} ```
